REMAINING TASKS — after Task 5 (Amara placeholder swap)
=============================================================
Task 2 is DONE (js/api.js client API layer):
- Mock driver: settings.guardianPin added; new studyBonusMinToday()/
  studyExtendCountToday() helpers mirror server.js exactly; new
  extendStudyTime(pin) method enforces the same PIN-match/daily-cap/
  fixed-grant rules locally.
- All 5 mock-driver call sites that compute studyWindowStatus() now pass
  the bonus minutes through (ping, catalogue gate, status read, start
  gate, getMySettings).
- Http driver: extendStudyTime(pin) added, posts to
  /me/study-time/extend.
- GOC.api facade: extendStudyTime forward added.
- test-driver-parity.js re-run: 8/8 pass, both drivers expose the new
  method. Full suite re-run: same pass/fail split as before
  (test-server.js / test-security.js still fail only for the
  pre-existing no-busboy/no-network environment reason) — no
  regressions.

Task 5 is DONE (cosmetic "Amara" placeholder):
- index.html, logged-out marketing landing page hero mockup: the
  hardcoded "Amara's progress" line was swapped to a neutral
  "Your progress" placeholder. One-line change, no other files touched.
- Left untouched, out of scope for this task: index.html's dashName
  element (id="dashName"), which shows "Amara" only in the static
  markup and is overwritten by JS at runtime with the real logged-in
  user's name — not the "real hardcoded" instance Task 5 named.

Task 3 is HALF DONE (js/app.js — the student-facing half only; the
admin half is still open, see below). Do NOT start Task 4 or Task 6
until told to proceed.

TASK 3 — js/app.js (client app logic)
--------------------------------------
DONE — student-facing half:
- paintStudyWindow(win): now reads win.bonusMin into
  state.studyBonusMin, and derives state.studyExtensionsLeft the same
  way server.js derives it (bonusMin / EXTRA_TIME_GRANT_MIN, capped by
  EXTRA_TIME_MAX_GRANTS_PER_DAY via rulebook()) rather than guessing it
  client-side — so the ring and wind-down copy can never promise a
  grant the server would refuse.
- paintWinddownText() now folds any bonus minutes already added today
  into the wind-down card's copy, and clears the (not-yet-existing)
  #windPinErr field so a stale message can't linger into the next time
  the overlay is shown.
- New swExtend() (alongside existing swStart/swStop/swPing): reads a
  PIN from #windPinInput, calls GOC.api.extendStudyTime(pin), repaints
  via paintStudyWindow() on success, and on a wrong PIN or exhausted
  daily grant count writes the server's own message into #windPinErr
  (never a toast, never a hint at the correct PIN or attempts left).
  #windPinInput / #windPinErr don't exist in index.html yet — Task 4
  adds them; swExtend() no-ops safely (shows "Enter the guardian PIN.")
  until then.
- node -c js/app.js: passes. Full test suite re-run: same pass/fail
  split as before (test-server.js / test-security.js still fail only
  for the pre-existing no-busboy/no-network environment reason;
  test-driver-parity.js still 8/8) — no regressions.

STILL OPEN — admin half (not started; do not start until told):
- loadSettings()/getSettings() for admins: read hasGuardianPin/
  guardianPin so the admin "window" screen can show/edit it (new admin
  fields + a setGuardianPin() call, alongside the existing
  adminSetLimit() pattern).

Still open, in the order they were originally listed (do NOT start any
of these until told to proceed):

TASK 4 — index.html (markup)
------------------------------
- Wind-down overlay: restore the PIN input + "Add 15 min" button
  (removed previously because it did nothing real), wired to swExtend(),
  shown only when hasGuardianPin is true. Use ids #windPinInput (the
  PIN field) and #windPinErr (the inline error text) so they line up
  with what swExtend()/paintWinddownText() already reference.
- Admin "Daily study window" card: add a field for setting/viewing the
  guardian PIN, next to the existing daily-limit stepper.

TASK 6 — Scripted ping-to-exhaustion simulation
--------------------------------------------------
- Not started. A new script in test/test-study.js's style should call
  swPing() enough times through the mock driver to cross the daily
  limit, then assert state.studyExhausted flips true and the ring/Start
  buttons/wind-down overlay all update.

Note: the unresolved Appwrite schema-gap risk from Task 1 still carries
forward — it's on the academy's side, not something further client work
here resolves.

Say "proceed" (or name the task number) when ready for the rest of
Task 3 (the admin half), or for Task 4.
