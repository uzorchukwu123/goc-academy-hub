PRACTICE SECTION EDIT — WHAT WAS DONE, AND WHAT IS STILL LEFT
==============================================================

THE REQUEST (as given)
  1. In the practice section, add a dropdown containing the choice of subjects,
     and a choice of number of questions by inputting the number.
  2. After starting the practice section, the student must be able to practice
     only ONE subject at a time — not two, and not all the subjects.
  Instruction: finish half, send the updated zip plus a .txt with the rest.

HALF DONE IN THIS PASS  (request item 1 — the set-up screen)
-------------------------------------------------------------
  index.html  (Study set-up screen, id="studySet")
    - Added a subject dropdown:  <select id="suSubjectDD"> inside #suSubjectDDWrap.
    - Added a number box:        <input type="number" id="suCountInput"> inside
      #suCountInputWrap, with a hint line #suCountHint.
    - The old chip strip (#suSubjects) and count chips (#suCounts) are kept, now
      inside #suSubjectChipsWrap / #suCountChipsWrap. They are hidden in practice
      and still used by the CBT set-up, which was NOT changed.

  js/app.js  (renderStudySet and helpers)
    - Practice mode: shows the dropdown (fed from the live subjects) and the
      number box; hides the chips. CBT mode is unchanged.
    - The typed number is kept between 1 and what the chosen subject holds
      (and never above the per-run maximum, 60, from studyCat.maxQuestions).
      Typing more than that is brought down to the limit with a short message.
    - An empty / 0 box disables Start ("Enter number of questions"); leaving
      the box empty restores the last valid number.
    - New functions: suPaintStart(), suCountCap(), suCountTyped(), suCountCommit().
      The Start button drawing was moved out of renderStudySet() into
      suPaintStart() so typing does not rebuild the box.
    - suStart() commits the typed number before starting.

  sw.js            CACHE bumped goc-v31 -> goc-v32 so students get the new files.
  test/test-study.js   The practice set-up assertions were rewritten for the
                       dropdown and number box (chips -> select, count chips ->
                       typed number, plus new "typing a number" checks).

  Verified: node test/run.js -> all 18 suites pass.

DELIBERATELY LEFT UNDONE  (request item 2 — one subject at a time)
-------------------------------------------------------------------
The dropdown currently STILL contains the "All my subjects" option, exactly as
the old chip strip did. Removing it, and enforcing single-subject practice, is
the remaining half:

  A. js/app.js — remove "All my subjects" from practice
       - renderStudySet(): delete the `if(live.length > 1){ opts.unshift(...) }`
         block that adds the "All my subjects" <option> (and the matching
         `chips.unshift` for CBT-side code, if any is reached in practice).
       - suIsAllSubjects(), the allSubjects branch in suAvailable(), and the
         PRACTICE_ALL_SUBJECTS check in suSetSubject() become dead code — remove.

  B. Server refusal (do not trust the screen)
       - server/server.js  (~line 2589, POST /me/study/start): in practice mode,
         reject subject === core.PRACTICE_ALL_SUBJECTS with a clear 400 error,
         and remove the `allSubjects` pool branch (studyPoolAcrossSubjects).
       - js/api.js  (~line 875, mock driver startStudy): same refusal, so the
         mock driver and the server keep matching (test-driver-parity.js).
       - js/goc-core.js: PRACTICE_ALL_SUBJECTS / studyPoolAcrossSubjects can then
         be removed or kept only as an unused export — decide when doing it.

  C. "After starting the practice section" — one subject per run
       - Confirm every question in a started practice run belongs to the single
         chosen subject (the server already builds the pool from one subject once
         "All my subjects" is refused).
       - Decide what should happen if a student tries to start a second practice
         run in a different subject while one is still open (block it, or let the
         new run replace the old one). The request does not say which — ask the
         academy before building it.

  D. Tests to change when A–C are done
       - test/test-study.js  (~lines 205-225: the startStudy "all subjects" pool
         checks, and the "practice offers an all my subjects option" block near
         line 396) — replace with checks that "All my subjects" is refused and
         is not in the dropdown.
       - test/test-server.js (~line 1327) — comment/assertions that mention the
         pooled practice run.
       - test/test-driver-parity.js — keep mock and server refusals identical.

  E. Bump sw.js CACHE again (goc-v32 -> goc-v33) when this half ships.

